[build]
  command = "npm run build"  # Replace with your build command if different
  functions = "netlify/functions"
  publish = "dist"           # Replace with your output folder (e.g., "build" for Create React App)
